import styles from '../../styles/page.module.css'

function GithubLoginButton() {
    return (
        <button id={styles.github_login_button}>Sign in with GitHub</button>
    )
}

export default GithubLoginButton;